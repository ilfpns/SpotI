/** The LP's center label default color, matching the original design. */
export const DEFAULT_LABEL_COLOR = "#f2f0ec";

/** The sleeve/case default color, matching the original design. */
export const DEFAULT_CASE_COLOR = "#f4f3f0";

/** Default popup/UI accent text color. */
export const DEFAULT_FONT_COLOR = "#f5f5f7";

/** Seven classic rainbow presets shown before the custom color wheel. */
export const RAINBOW_PRESETS: string[] = [
  "#ef4444", // red
  "#f97316", // orange
  "#eab308", // yellow
  "#22c55e", // green
  "#3b82f6", // blue
  "#4338ca", // indigo
  "#a855f7", // violet
];

export type UiTheme = "dark" | "light";
export const DEFAULT_UI_THEME: UiTheme = "dark";

/** Whether the case and LP each show their thin outline stroke. */
export const DEFAULT_SHOW_BORDER = true;
