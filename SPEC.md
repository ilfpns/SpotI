# SpotI 기능 명세서

Electron + TypeScript로 만든 데스크톱 Spotify 동반자 앱. 이 문서는 실제
구현된 코드를 기준으로 작성되었습니다 (설계 의도가 아니라 현재 동작).

## 1. 개요

- **형태**: 케이스에서 반쯤 꺼낸 LP 모양의 항상-위-표시(always-on-top),
  프레임 없는 투명 창. 창 자체가 작아서(48-88px) 클릭통과는 쓰지 않고
  항상 자기 자신의 영역을 그대로 캡처함 — 펫이 놓인 자리 밑의 아이콘/창이
  실수로 클릭되는 일이 없음.
- **핵심 동작**: 드래그로 어디든 이동 가능. 마우스를 올리면(hover) 근처에
  현재 재생 정보 + 컨트롤 팝업이 나타남.
- **인증**: 사용자 자신의 Spotify 앱 Client ID로 PKCE(Authorization Code
  + PKCE) 플로우 수행. 클라이언트 시크릿을 앱에 내장하지 않음.

## 2. 아키텍처

### 2.1 프로세스/창 구성

| 창 | 크기 | 특징 |
|---|---|---|
| Pet | 48/64/88px (설정값) | 투명, 프레임 없음, 항상 위, 클릭통과 없음(자기 영역 항상 캡처), 크기·불투명도 실시간 반영 |
| Popup | 360×190 | 호버 시에만 표시, 펫 위/아래 자동 배치 |
| Settings | 860×560 | 일반/테마/케이스/기록/Spotify 5개 탭 |
| Context Menu | 168×122, 직각 모서리 | 우클릭 시 지연 생성(lazy), 블러 시 자동 숨김, 설정/Spotify 바로가기/종료 |

각 창은 `contextIsolation: true`, `nodeIntegration: false`로 통일. Preload
스크립트(`src/preload/preload.ts`)가 `window.petAPI`로 타입이 지정된 IPC
표면만 노출.

### 2.1.1 앱 인스턴스 & 위치 기억

- `app.requestSingleInstanceLock()`으로 중복 실행 방지 — 이미 실행 중일 때
  다시 실행하면 기존 펫 창을 보여주고 새 인스턴스는 즉시 종료.
- 펫을 드래그해 옮긴 위치는 `appSettingsStore`(`settings.json`)에 저장되고
  다음 실행 시 복원됨. 저장된 위치가 현재 연결된 모니터의 작업 영역
  밖이면(모니터 구성이 바뀐 경우 등) 기본 우하단 위치로 폴백.

### 2.2 IPC

`src/shared/ipcChannels.ts`에 채널명이 단일 소스로 정의되어 있고,
`preload.ts`와 `main/ipc/registerIpcHandlers.ts` 양쪽에서 이를 참조.
설정값은 `invoke`(요청-응답), 실시간 브로드캐스트는 `send`/`on` 패턴 사용.

### 2.3 팝업 표시/숨김 상태 머신

`src/main/popupController.ts`가 `hidden → visible → grace → closing →
hidden` 상태를 명시적으로 관리(60ms 간격으로 커서 위치 폴링). 이전에는
`win.isVisible()`과 타이머 존재 여부를 섞어서 판단하다가, 사라지는
애니메이션 도중 커서가 돌아오면 팝업이 "떠 있지만 사라진 것처럼 보이는"
버그가 있었음 — 이번에 상태 머신으로 재작성해 수정. 자동화 테스트:
`src/main/popupController.test.ts` (빠른/느린/무작위 토글 6개 시나리오).

## 3. 핵심 기능

### 3.1 펫

- 드래그 이동, 드래그 중에도 크기가 틀어지지 않도록 항상 명시적
  width/height로 `setBounds()` (Windows에서 transparent 창에
  `setPosition()`만 반복 호출 시 보고되는 너비가 깨지는 문제 회피).
  포인터 이동량이 임계값(4px) 이하면 드래그가 아닌 클릭으로 판정.
- 우클릭 → 커스텀 컨텍스트 메뉴(설정 열기 / Spotify 바로가기 / 종료).
- **클릭 시 케이스가 슬라이드되어 LP가 전부 드러남**(다시 클릭하면
  원위치). 직선 대각선이 아니라 꺾인 경로 — 벗겨질 때는 먼저 왼쪽으로
  이동한 다음 아래로 슬라이드되며 사라지고, 다시 씌울 때는 같은 경로를
  반대로 거슬러(먼저 위로 올라온 다음 오른쪽으로 이동) LP와 포개어지는
  자리로 돌아옴. 이런 꺾인 경로는 CSS `transition` 하나로는 표현할 수
  없어(직선 보간만 가능) `@keyframes` 두 개(`pet-case-away`/
  `pet-case-home`)로 구현하고, 클릭할 때마다 JS(`pet/main.ts`)가 두 클래스
  중 하나를 강제 리플로우와 함께 갈아끼워 애니메이션 도중에 다시 클릭해도
  깔끔하게 재시작됨. 슬라이드 속도는 설정에서 조절 가능(애니메이션 탭,
  4.4절 참고).
- **케이스 상태 = 실제 재생 상태**: 케이스를 벗기면 재생, 씌우면
  일시정지가 실제로 호출됨(`window.petAPI.spotify.play()`/`pause()`).
  반대 방향도 마찬가지 — 팝업의 재생/일시정지 버튼, 미디어 키, 다른
  기기에서의 재생 상태 변경 등 무엇으로 바뀌었든 `nowPlayingChanged`
  이벤트를 받아 케이스가 실제 `isPlaying`을 그대로 따라감(클릭은
  낙관적으로 로컬 상태를 먼저 바꾸고, 이 이벤트가 나중에 그걸 다시
  확인/보정하는 식이라 서로 충돌하지 않음). 이 이벤트가 팝업 창에만
  가던 것을 모든 창에 방송하도록 고친 게 이 기능이 실제로 동작하기 위한
  전제 조건이었음(7절 참고).
- 케이스가 벗겨져 있는 동안에만 LP가 회전 — 회전은 CSS 애니메이션이
  아니라 `requestAnimationFrame`으로 각도를 직접 누적시켜, 케이스를 다시
  씌우면 그 자리에서 즉시 정지하고 다음에 벗길 때 같은 각도부터 이어서
  회전(0도로 되돌아가지 않음). 벗겨지는 순간에는 설정된 기본 속도보다
  한동안 더 빠르게(기본 속도의 최대 4배) 돌다가 지수적으로 감쇠하며
  약 1초에 걸쳐 기본 속도로 자연스럽게 잦아듦 — 일정한 속도로 바로
  튀어나오는 대신, 케이스를 벗기는 손짓에 LP가 한 번 튕겨나가는 느낌을
  줌. 기본 회전 속도 자체도 설정에서 조절 가능(4.4절).
- LP 표면은 동심원 그루브(여러 개의 얇은 원) + 여러 개의 얇은 대각선
  스트로크로 만든 은은한 빛 반사 효과로 실제 바이닐처럼 보이게 처리.
- **케이스 모양**: 두 가지 중 선택 가능(`pet-case`가 `<rect>`가 아니라
  `<path>`) — 케이스 탭에서 바꿀 수 있음(4.3절 참고).
- 케이스에 사용자가 지정한 이름을 새길 수 있음(케이스 탭, 4.3절 참고).
- SVG 하나(`src/shared/petSvg.ts`)가 펫 렌더러, 트레이 아이콘, 설정창
  브랜드 아이콘에 모두 재사용됨 — 트레이 아이콘은 Chromium으로 오프스크린
  렌더링 후 rasterize.

### 3.2 팝업 (재생 컨트롤)

- 앨범 아트, 곡명(마퀴 스크롤), 아티스트(말줄임), 셔플/이전곡/재생·일시
  정지/다음곡/반복(반복은 꺼짐→전체반복→한곡반복 순환) 5버튼, 탐색
  가능한 진행 바, 상태 낙관적 갱신(optimistic update) 후 즉시 재폴링으로
  실제 상태와 동기화.
- 앨범 아트 디스크 회전도 펫의 LP와 같은 방식(`requestAnimationFrame`
  으로 각도 직접 누적)으로 처리 — 일시정지하면 그 각도에서 바로 멈추고
  다시 재생하면 그 각도부터 이어서 회전. DOM 엘리먼트도 재생/일시정지
  때마다 새로 만들지 않고 그대로 재사용해 회전 상태가 끊기지 않음.
- Spotify 미연결 시 연결 버튼 표시.
- 곡명 오른쪽에 하트 버튼 — Spotify의 실제 Liked Songs에 저장/제거
  (`PUT`/`DELETE /me/tracks`). 클릭 시 낙관적으로 하트부터 빨갛게 채우고,
  실패하면(그리고 그 사이 곡이 안 바뀌었으면) 되돌림. 트랙이 바뀔
  때마다 한 번씩만 `GET /me/tracks/contains`로 저장 여부를 새로 확인.
  설정 창을 열거나 하지는 않음 — 즐겨찾기 목록은 설정 → 즐겨찾기 탭에서
  따로 확인(4.7절).
  **Liked Songs API는 `user-library-modify`/`user-library-read` 스코프가
  필요** — 이 기능을 추가하며 `SPOTIFY_SCOPES`에 새로 넣었으므로,
  이전에 이미 로그인해 둔 계정은 Settings → Spotify에서 로그아웃 후
  다시 연결해야 하트가 동작함(기존 리프레시 토큰은 새 스코프를 못
  받아옴 — Spotify OAuth의 일반적인 동작).

### 3.3 미디어 키

`src/main/mediaKeys.ts` — `globalShortcut`으로 `MediaPlayPause`/
`MediaNextTrack`/`MediaPreviousTrack`을 전역 등록. 설정에서 켜고 끌 수
있음(다른 앱과 충돌 시 대비).

## 4. 설정 (Settings)

### 4.1 일반

| 항목 | 저장 위치 | 기본값 |
|---|---|---|
| 언어 | `localeStore` (`locale.json`) | ko |
| 볼륨 | Spotify API 직접 반영 (로컬 저장 없음) | — |
| 불투명도 | `appSettingsStore` (`settings.json`) | 100 (최소 20으로 하한) |
| 시작 시 자동 실행 | OS 로그인 항목(`app.setLoginItemSettings`) | false |
| SpotI 크기 | `appSettingsStore` | medium(64px) |
| 트랙 변경 알림 | `appSettingsStore` | true |
| 알림 소리 | `appSettingsStore` | true |
| 연동(폴링) 속도 | `appSettingsStore` | fast(200ms 활성/3000ms 유휴) |
| 팝업 반응(소멸) 속도 | `appSettingsStore` | normal(60ms) |
| 디스크 회전 애니메이션 | `appSettingsStore` | true |
| 미디어 키 지원 | `appSettingsStore` | true |
| 시작 시 숨기기 | `appSettingsStore` | false |
| 설정 초기화 | — | 버튼 (아래 4.5 참고) |

### 4.2 테마

| 항목 | 저장 위치 | 기본값 |
|---|---|---|
| 화면 모드 (다크/라이트/**시스템 설정**) | `themeStore` (`theme.json`) | dark |
| 폰트 컬러 (팝업 텍스트/아이콘) | `themeStore` | `#ffffff` |
| LP 컬러 (라벨 원) | `themeStore` | `#22c55e` |
| 재생 중인 곡 컬러 따라가기 | `themeStore`의 `followNowPlayingColor` | false |
| 테두리 표시 여부 | `themeStore` | true |
| 테두리 색 | `themeStore` | `#2e2e2e` |

**재생 중인 곡 컬러 따라가기**: 켜면 LP 컬러 프리셋/휠 대신, 현재
재생 중인 곡의 앨범 아트에서 뽑은 색이 LP 컬러로 자동 적용됨. 색 추출은
팝업 렌더러(`renderer/popup/dominantColor.ts`)에서 이미 로드된 앨범 아트
`<img>`를 작은 오프스크린 캔버스(24×24)에 그려 픽셀을 평균 내되, 채도가
높고 너무 밝거나 어둡지 않은 픽셀에 가중치를 줘서 큰 배경보다 실제
색감이 있는 부분이 우세하도록 함(별도 이미지 처리 라이브러리 없이 순수
Canvas API만 사용). 트랙이 바뀔 때마다(재생 중 상태 변화가 아니라
`albumArtUrl` 자체가 바뀔 때만) 추출해 `setLabelColor()`를 호출 —
팝업이 화면에 보이지 않아도 항상 로드되어 있으므로 백그라운드에서도
동작함. CORS로 인해 캔버스 읽기가 막히면(taint) 조용히 실패하고 이전
색을 유지함. 팝업 창도 펫 창과 마찬가지로 `backgroundThrottling: false`로
생성됨 — 팝업은 평소 숨겨져 있다가 호버할 때만 보이는데, Chromium
기본값대로면 숨겨진 동안 색 추출이 실제 트랙 변경보다 눈에 띄게 느리게
반영될 수 있음.

켜는 순간의 LP 컬러를 `themeStore`의 `preFollowLabelColor`에 저장해뒀다가,
끄면 그 색으로 되돌림(메인 프로세스의 `setFollowNowPlayingColor` IPC
핸들러가 처리) — 꺼도 마지막으로 추출된 색에 그대로 머물러 있지 않음.

색상은 무지개 프리셋 7개 + 커스텀 컬러 휠로 선택. 변경 시 모든 열린 창에
브로드캐스트되고, 트레이/설정창 아이콘도 재생성됨.

화면 모드를 "시스템 설정"으로 두면 저장되는 값은 `"system"`이고, 각 창에
실제로 적용되는 다크/라이트는 메인 프로세스가 Electron `nativeTheme`으로
매번 해석(`getEffectiveUiTheme`)해서 별도 채널로 방송함 — OS 테마가
바뀌면 `nativeTheme`의 `updated` 이벤트를 받아 열려 있는 모든 창에 다시
방송. 설정 화면에는 사용자가 고른 원래 값(다크/라이트/시스템)이 그대로
표시되고, 나머지 창들은 해석된 값만 받음.

### 4.3 케이스

| 항목 | 저장 위치 | 기본값 |
|---|---|---|
| 케이스 모양 | `themeStore`의 `caseShape` | `cut` |
| 케이스 컬러 | `themeStore` | `#595d64` |
| LP 이름 | `themeStore`의 `discName` | `""` (비어있으면 표시 안 함) |

- **케이스 모양**: 두 가지 중 선택.
  - `classic`: 케이스 자신의 높이와 같은 42×42 정사각형(원래는 세로보다
    좁은 직사각형이었음). 오른쪽 끝은 LP의 가로 중심(x=39)에 그대로
    고정돼 있어 정사각형으로 넓어져도 LP가 정확히 절반만 드러나는 원래
    비율은 그대로 유지되고, 넓어진 만큼은 왼쪽으로만 늘어남(아이콘의
    왼쪽 경계를 살짝 넘어감).
  - `cut`(기본값): LP의 바운딩 박스와 정확히 같은 42×42 정사각형 —
    케이스와 LP가 완전히 포개어져 있고, 그 정사각형의 대각선 두 개가
    만나는 중심(O)을 꼭짓점으로, 오른쪽 위 모서리(D)와 오른쪽 아래
    모서리(C)를 잇는 삼각형(D-O-C) 하나만 잘라낸 모양 — 그 삼각형
    자리로만 LP가 보임(케이스를 슬라이드해서 벗기기 전에도).
  - 두 모양 모두 `shared/petSvg.ts`의 `casePathFor()` 하나가 만들어내는
    `<path>` `d` 값만 다를 뿐, 설정 저장·트레이 아이콘 재생성 로직은
    모양과 무관하게 동일하게 동작함. 케이스를 슬라이드해서 벗기는
    애니메이션은 왼쪽으로 75px, 아래로 90px 이동하는데(3.1절의 꺾인
    경로 참고), 두 모양 중 더 넓은 쪽(`cut`, 왼쪽 끝 x=18 / `classic`,
    왼쪽 끝 x=-3) 기준 가로 이동만으로도 이미 화면 밖으로 완전히
    빠져나가는 여유값이라 어느 모양이든 확실히 사라짐.
- 케이스 컬러 선택기는 원래 테마 탭에 있었으나 별도 탭으로 분리됨.
- LP 이름은 영문·숫자·공백·일부 기호(``!?.,'"&#@_-``)만 허용, 최대
  7자로 서버(메인 프로세스)와 입력창 양쪽에서 동일한 sanitize 함수
  (`shared/theme.ts`의 `sanitizeDiscName`)로 검증. 케이스 위에 새겨져
  케이스와 한 몸으로 움직임 — 케이스가 벗겨질 때 이름도 함께 슬라이드되어
  나가고, 케이스 색과 자동으로 대비되는 글자색이 선택됨(밝은 케이스엔
  어두운 글자, 어두운 케이스엔 밝은 글자).

### 4.4 애니메이션

| 항목 | 저장 위치 | 기본값 |
|---|---|---|
| 케이스 여닫는 속도 | `appSettingsStore`의 `caseSlideSpeed` | `normal` (380ms) |
| LP 회전 속도 | `appSettingsStore`의 `discSpinSpeed` | `normal` (펫 120°/s, 팝업 18°/s) |

느림/보통/빠름 3단계, 다른 속도 설정(연동 속도 등)과 같은 세그먼트
컨트롤 UI. LP 회전 속도는 펫의 작은 LP와 팝업의 큰 앨범 아트 디스크가
서로 다른 기본 회전 속도(펫이 더 빠름 — 원래도 그렇게 디자인됨)를 갖고
있어, 값 하나가 두 `Record<DiscSpinSpeed, number>` 맵
(`shared/constants.ts`의 `PET_DISC_SPIN_DEG_PER_SEC` /
`POPUP_DISC_SPIN_DEG_PER_SEC`)을 통해 각 창에 서로 다른 실제 값으로
적용됨. 케이스를 벗기는 순간의 "스핀업" 배율(최대 4배, 3.1절 참고)은
이 설정과 별개로 항상 고정.

### 4.5 기록 (Listening History)

- `src/main/listeningHistoryStore.ts`가 재생 중(polling tick마다) 실제
  경과 시간을 트랙별/일자별로 누적 기록 (`history.json`).
- 보관 기간: 5년 + 30일 여유. 연도별 히트맵 전환 기능이 실제로 넘겨볼
  과거 연도를 가지려면 364일치만 보관하는 걸로는 부족해서, 이번에
  기존의 롤링 윈도우(364일) 대신 연 단위 보관으로 확장됨. 그 이전
  데이터는 자동 정리(prune)되어 메모리/디스크가 무한정 커지지 않음.
- 설정 화면: 총 청취 시간/이번 주/베스트 데이/일 평균/**재생 횟수**/
  최장·현재 연속 기록, **GitHub 잔디 스타일 연도 선택 드롭다운**(기록이
  있는 연도 + 현재 연도, 기본값은 현재 연도)으로 전환되는 2D 캘린더
  히트맵(선택한 연도의 1월 1일~12월 31일, 스크롤 없이 한 화면에 표시),
  클릭한 날짜의 베스트 트랙(가장 오래 들은 곡) 표시.
- **재생 횟수**: 청취 시간(ms 누적)과는 별개 지표 — 트랙이 실제로
  바뀌면서 재생 중인 순간(`pollingService.tick()`의 기존 `trackChanged`
  감지를 그대로 재사용, 앱을 막 시작한 직후의 "첫 폴링"은 제외)마다
  하루치 전체 카운터와 그 트랙 자신의 카운터를 함께 1씩 올림
  (`listeningHistoryStore.recordTrackStart(trackId, ...)`) — 전자는
  이 페이지의 "재생 횟수" 통계에, 후자는 4.7절 즐겨찾기 목록의 트랙별
  통계(`getTrackStats(trackId)`)에 쓰임.

### 4.6 Spotify

- **Client ID 설정**: 앱 내에서 직접 입력·저장 (`userData/
  spotify.config.json`, 패키징된 앱에서도 쓰기 가능한 위치). 개발자
  대시보드 바로가기 버튼, Redirect URI 값 표시.
  - `SPOTIFY_CLIENT_ID` 환경 변수가 있으면 이 값이 우선 적용(개발자용).
- 연결 상태 표시, 연결 해제 버튼.
- 버전/런타임 정보(Electron·Chromium·플랫폼), **업데이트 확인** 버튼
  (`src/main/updateChecker.ts` — GitHub Releases API로 최신 릴리즈를
  조회해 semver 비교, 배포된 릴리즈가 아직 없으면 "확인할 수 없음"으로
  처리되는 게 정상 동작).

### 4.7 즐겨찾기 (Favorite)

- 팝업의 하트 버튼(3.2절)으로 저장한 Spotify Liked Songs 목록을 보여줌
  — 목록 자체는 로컬에 별도로 저장하지 않고 매번 `GET /me/tracks`를
  페이지 단위(20개)로 불러와 항상 Spotify 쪽 실제 상태를 그대로 반영.
  - 각 행: 앨범 아트, 곡명/아티스트, **재생 횟수·총 청취 시간**(로컬
    `listeningHistoryStore.getTrackStats(trackId)` — 저장된 모든
    날짜의 기록을 그 트랙 기준으로 합산, Spotify가 아니라 이 앱이
    직접 재생을 관찰한 기록에서 나오는 값이라 실제로 SpotI로 들은
    적 없는 곡은 0으로 표시됨), 제거 버튼(`DELETE /me/tracks`, 낙관적
    으로 목록에서 먼저 지우고 실패하면 전체 재로딩으로 복구).
  - "더 보기" 버튼으로 다음 페이지 로드.

### 4.8 설정 초기화

"초기화" 버튼은 다음 값들로 되돌림(주의: 앱 최초 설치 시 기본값과 다른
경우가 있을 수 있음 — 사용자가 이 버튼에 명시적으로 요청한 값):
언어=영어, LP/케이스/폰트/테두리 컬러=앱 기본값(위 4.2·4.3표 참고),
화면 모드=다크, 테두리 표시=켜짐, LP 이름=빈 값,
재생 중인 곡 컬러 따라가기=꺼짐, 케이스 모양=`cut`, 크기=medium,
자동실행=꺼짐, 알림=켜짐, 연동속도=fast, 팝업반응=normal,
애니메이션(디스크 회전 on/off)=켜짐, 케이스 여닫는 속도=normal,
LP 회전 속도=normal, 미디어키=켜짐, 알림소리=켜짐, 시작시숨기기=꺼짐,
불투명도=100. Spotify 연결 상태는 그대로 유지됨.

## 5. 다국어 (i18n)

- `src/shared/i18n.ts`에 ko/en/zh/ja 4개 언어 사전이 하나의 파일에 정의.
- 렌더러: `data-i18n` 속성 스캔 방식(`renderer/i18nClient.ts`).
- 메인 프로세스: 트레이 메뉴, 트랙 변경 알림 등도 `translate()` +
  `localeStore.getLocale()`로 동일하게 로컬라이즈.
- 언어 변경 시 모든 창 + 트레이 메뉴가 즉시 갱신됨.

## 6. 보안

- **인증**: PKCE(Authorization Code + PKCE), 클라이언트 시크릿 없음.
  Refresh token은 Electron `safeStorage`로 암호화해 디스크에 저장
  (`tokenStore.ts`). Access token은 메인 프로세스 메모리에만 존재하며
  렌더러로 절대 전달되지 않음.
- **CSP**: 패키징된 빌드에서 `session.defaultSession.webRequest
  .onHeadersReceived`로 실제 Content-Security-Policy 헤더 적용
  (`default-src 'self'`, 원격 스크립트/스타일 출처 없음, 앨범 아트용
  `https:` 이미지만 허용). 개발 모드는 Vite HMR을 위해 적용 안 함.
- **창 보안**: 모든 창(펫/팝업/설정/컨텍스트메뉴 + 트레이 아이콘 렌더링용
  오프스크린 창) `contextIsolation: true` / `nodeIntegration: false`,
  `sandbox`는 Electron 33 기본값(true) 그대로 예외 없음. `src/main/
  windowSecurity.ts`의 `hardenWindow()`를 모든 창 생성 직후 호출해
  `will-navigate`와 새 창 열기를 전부 거부 — 이 앱은 자기 자신의 번들
  HTML만 로드하고 외부 페이지로 이동할 일이 전혀 없으므로, 뭔가가
  탐색을 시도하더라도(버그든 뭐든) 조용히 막힘.
- **외부 URL 열기**: Spotify 대시보드/릴리즈 페이지 링크는 렌더러가 URL을
  넘기는 게 아니라, 메인 프로세스에 고정된 주소만 여는 전용 IPC 채널
  사용(렌더러가 임의 URL을 열게 할 수 없음).
- **XSS**: Spotify API에서 온 곡명/아티스트/앨범아트 URL은 모두
  `escapeHtml()`을 거쳐서만 `innerHTML`에 들어감 — 텍스트 콘텐츠뿐
  아니라 `<img src="...">` 같은 속성 위치에 들어가는 URL도 마찬가지
  (속성값 안에서 따옴표로 깨져나가는 걸 막기 위해). 팝업의 앨범 아트
  `<img>`는 `.src` 프로퍼티로 직접 대입(HTML 문자열 조립이 아님)해서
  애초에 이 문제가 없음. LP 이름은 허용 문자 집합 자체가 `<`/`>`를
  빼놓고 있고, SVG 텍스트 콘텐츠로만 삽입되며(속성 값 아님) `&`만
  이스케이프하면 충분.
- **비밀정보**: 저장소에 시크릿 없음(소스 스캔으로 확인). 개발용
  `spotify.config.json`은 `.gitignore` 처리.

## 7. 성능/최적화

- **폴링**: 설정의 폴링 속도(fast=200ms/normal=1000ms)가 팝업이 열려
  있는지와 무관하게 항상 그대로 적용됨. 예전엔 팝업이 닫혀있을 때
  (즉 펫이 대부분의 시간을 보내는 상태)만 고정 3000ms 유휴 간격을 써서
  "빠름"을 골라도 체감 속도가 거의 안 바뀌었음 — "연동 속도를 최대로"
  요청에 맞춰, 유휴/활성 구분을 없애고 언제나 설정값 그대로 폴링하도록
  단순화(`pollingService.applyInterval()`). `startPolling()` 시작 시에도
  이 값을 바로 적용하도록 함께 고침 — 이전엔 팝업 가시성이 한 번 바뀔
  때까지 저장된 속도 설정이 아예 반영 안 되는 미묘한 시작 시점 공백이
  있었음. 429 레이트리밋 응답 시 로컬에서 일정 시간 요청을 멈추는
  회로차단기(circuit breaker)는 여전히 적용.
- **창 생성**: 컨텍스트 메뉴 창은 지연 생성(첫 사용 시에만).
- **아이콘**: 트레이/윈도우 아이콘은 캐시 후 테마 변경 시에만 재생성.
- **기록 저장소**: 쓰기는 5초 배치(디바운스), 앱 종료 시 즉시 flush.
  오래된 기록은 자동 정리(위 4.5 참고).
- **디스크 회전**: `requestAnimationFrame`은 케이스가 벗겨져 있을 때만
  실행되고, 다시 씌우면 즉시 정지 — 유휴 상태에서 불필요한 리페인트가
  계속 발생하지 않음.
- **볼륨 조회 / 미디어 키 재생·일시정지**: 둘 다 별도로 `/me/player`를
  다시 호출하지 않고, 폴링 루프가 이미 받아온 최신 상태를 재사용
  (`pollingService.getLastKnownState()`) — 설정 화면에서 볼륨을 읽을 때,
  그리고 하드웨어 미디어 키로 재생/일시정지를 누를 때 현재 재생 상태를
  판단하려고 매번 새 네트워크 왕복을 만들지 않음.
- **펫 창의 backgroundThrottling**: 펫 창은 `webPreferences.
  backgroundThrottling: false`로 생성됨 — 항상 위(always-on-top) 창이라도
  OS/Chromium이 다른 창에 가려졌다고(occluded) 판단하면 기본적으로
  `requestAnimationFrame`이 통째로 멈추는데, 이러면 케이스를 벗겨도
  디스크가 회전하지 않거나 호버 감지가 끊기는 문제가 생김. 백그라운드
  스로틀링을 꺼서 항상 정상 동작하도록 한 대신, 완전히 가려져 있을 때도
  약간의 CPU를 계속 씀 — 펫 창 하나에 한정된 트레이드오프. 팝업 창도
  마찬가지로 꺼져 있음(평소 숨겨져 있다가 호버할 때만 보이는 창이라
  똑같은 문제가 있었음).
- **`nowPlayingChanged`를 모든 창에 방송**: 원래는 폴링 루프가 이 이벤트를
  팝업 창 하나에만 보내고 있었음 — 펫의 케이스가 실제 재생 상태를
  따라가는 기능을 붙이면서 드러난 버그로, 펫 창은 이 이벤트를 아예 받지
  못해 케이스 동기화가 전혀 동작하지 않았음. `BrowserWindow.
  getAllWindows()`로 모든 창에 보내도록 고침.
- **재생/일시정지 낙관적 방송**: `spotifyApiClient.play()`/`pause()`
  호출이 성공하면, 뒤이은 `pollNow()`의 실제 폴링(Spotify에 또 한 번
  네트워크 왕복)을 기다리지 않고 이미 알고 있는 새 `isPlaying` 값을
  즉시 모든 창에 방송함(`pollingService.
  broadcastOptimisticPlayState()`) — `pollNow()`는 그대로 뒤따라가서
  진행률 등 다른 값도 갱신하지만, 재생/일시정지 자체를 각 창이 알게
  되는 데는 더 이상 두 번째 네트워크 왕복을 기다리지 않음. 미디어 키의
  재생/일시정지 토글에도 동일하게 적용. 실측: 이 방송은 API 호출이
  끝난 뒤 10ms 안팎 만에 펫 창의 케이스 상태를 바꿔놓음(이전에는 별도의
  네트워크 왕복 하나가 통째로 더 걸렸음).
- **낙관적 방송 이후의 두 가지 경합(race) 수정**: 위 낙관적 방송을 붙인
  뒤, 팝업 창에서 재생/정지를 누르면 케이스가 순간적으로 껴졌다
  빠졌다 하다 결국 올바른 상태로 정착하는 깜빡임 버그가 있었음
  (`pollingService.ts`).
  - **요청 토큰(`requestToken`)**: 이미 진행 중이던 정기 폴링
    `tick()`이, 액션이 Spotify 서버에 실제로 반영되기 *전* 시점에 조회를
    시작했다면, 그 응답은 낙관적 방송보다 늦게 도착해서 막 방송한 옳은
    값을 오래된 값으로 덮어씀. 매 `tick()` 시작 시 토큰을 하나 증가시켜
    자기 것으로 갖고, 응답이 왔을 때 그 토큰이 여전히 최신인 경우에만
    반영/방송하도록 함. `broadcastOptimisticPlayState()`도 호출될 때
    토큰을 증가시켜, 그 시점에 이미 날아가 있던(in-flight) 폴링을
    무효화함.
  - **낙관적 가드 윈도(`OPTIMISTIC_GUARD_MS`, 1.5초)**: 요청 토큰만으로는
    못 잡는 두 번째 경합 — 액션 성공 직후 뒤이어 부르는 `pollNow()`가
    (토큰상으로는 최신이라도) Spotify 서버에 아직 전파되지 않은
    반영-이전 `isPlaying` 값을 그대로 GET 해오는 경우가 실제로 있었음.
    `broadcastOptimisticPlayState()` 호출 후 1.5초 동안은, 폴링 결과의
    `isPlaying`이 방금 확정 방송한 값과 다르면 전파 지연으로 간주하고
    그 필드만 낙관적 값으로 덮어씀(트랙 변경 등 다른 필드는 그대로 폴링
    결과를 따름). CDP로 실제 Spotify 세션에 대고 팝업 창의
    재생/정지 버튼과 빠른 연속 토글(150ms 간격)까지 재현해 깜빡임이
    사라졌음을 확인함.
- **`lastState` 캐시가 트랙/재생 상태 변화가 없으면 갱신되지 않던 문제**:
  전체 기능 점검 중 발견. `statesEqual()`(트랙ID/재생여부만 비교)이
  방송 여부뿐 아니라 `lastState` 자체를 갱신할지도 같이 gating하고
  있었음 — 트랙이나 재생 여부가 안 바뀌면 매 폴링이 최신 볼륨/셔플/
  반복 값을 받아오고도 `lastState`엔 반영하지 않고 버렸음.
  `getLastKnownState()`를 그대로 쓰는 `settings:get-volume` 핸들러가
  이 캐시를 우선 사용하기 때문에, 곡을 안 바꾼 채로 볼륨을 바꾸면
  `getVolume()`이 몇 번을 다시 불러도 계속 예전 값을 돌려줬음(실측
  재현: setVolume 후 폴링 주기를 넘겨도 값이 그대로였다가, 이 수정
  후엔 다음 폴링에서 정확히 갱신됨). 수정: `lastState` 갱신은
  매 성공한 폴링마다 항상 하고, 방송 여부만 `statesEqual()`로 계속
  gating.
- **셔플/반복 토글이 실패해도 팝업 UI가 계속 틀린 상태를 보여주던 문제**:
  같은 점검에서 발견. 팝업의 셔플/반복 버튼은 클릭 시 낙관적으로 먼저
  바뀌고, 다음 `onNowPlayingChanged` 방송으로 실제 상태와 맞춰짐(재생/
  정지 버튼과 같은 패턴) — 그런데 `statesEqual()`이 셔플/반복 값을
  안 봐서, 트랙·재생 여부가 그대로인 채 셔플/반복만 바뀌면 방송 자체가
  안 나갔음. 즉 Spotify가 토글을 거부하거나 다른 값으로 덮어써도 팝업은
  영영 고쳐지지 않고 틀린 낙관적 상태를 계속 보여줌. `statesEqual()`에
  `shuffleState`/`repeatState` 비교를 추가해 고침(진행률/볼륨은 여전히
  제외 — 진행률은 방송이 아니라 클라이언트에서 보간하고, 볼륨은
  `getVolume()`의 직접 조회 경로가 따로 있음).
- **회귀 테스트**: `pollingService.test.ts`에 위 두 항목을 포함해 총
  7개 테스트 추가(요청 토큰 경합 2개, 가드 윈도 2개, 캐시 신선도 1개,
  셔플/반복 방송 2개) — 자세한 내용은 9. 테스트 참고.
- **펫이 화면 밖으로 나가 "사라지는" 문제**: 사용자가 "SpotI가 안
  움직임/클릭이 다 안 됨"으로 보고 — 실제 원인은 클릭/애니메이션 로직이
  아니라, 펫 창이 어떤 경로로든(저장된 좌표 복원, 드래그 중 좌표 이상)
  현재 연결된 모든 모니터의 작업 영역 밖으로 나가버려 화면에 아예
  보이지 않게 된 것이었음(`petWindow.ts`). 두 군데를 고침:
  - **창 생성 시 저장된 좌표 복원**: 기존엔 `screen.getPrimaryDisplay()`
    한 대만 검사해서, 저장된 좌표가 (지금은 연결 안 된 모니터 등으로)
    primary 밖이면 무조건 기본 위치로 되돌렸음 — `screen.getAllDisplays()`
    전체를 검사하는 `isOnAnyDisplay()`로 교체.
  - **드래그 중 좌표(`moveTo` IPC)**: 저장된 값뿐 아니라 실시간 드래그
    좌표도 그대로 믿지 않고, `clampToVisibleArea()`로 가장 가까운
    연결된 모니터의 작업 영역 안쪽으로 항상 clamp 후 적용하도록 함 —
    바로 위 `moveTo` 핸들러의 기존 주석에 이미 있던, "이 Windows
    환경에서 드래그 중 반복된 setBounds() 호출이 창 크기를 조용히
    깨뜨리는 걸 관찰했다"는 것과 같은 계열의 문제로 보고, 좌표에도 같은
    방어적 보정을 적용.

- **재생 중인 트랙이 아예 없을 때 클릭하면 케이스가 헛되이 벗겨지던 문제**:
  펫 클릭은 낙관적으로 케이스부터 벗기고 `play()`를 부르는데, 애초에
  로드된 트랙이 하나도 없으면(일시정지된 곡조차 없음) 재개할 대상이
  없어 `play()`가 의미가 없고, 다음 폴링에서 결국 다시 덮이는 헛동작만
  됨. `pet/main.ts`에 `hasTrack`(현재 트랙 존재 여부, `onNowPlayingChanged`로
  갱신)을 추가해, 트랙이 없는 상태에서의 클릭은 케이스를 아예 안 벗기고
  API 호출도 안 하도록 함. 재생 중이던 걸 일시정지해 다시 누르는
  일반적인 케이스(트랙은 있고 일시정지만 된 상태)는 그대로 동작.
- **한쪽 화면 끝에 걸린 뒤로 드래그가 가로로만 안 움직이던 문제**: 위
  `clampToVisibleArea()`를 붙인 직후 새로 생긴 회귀. `hitTest.ts`의
  드래그는 시작 시점 창 위치(`winX`/`winY`, 페이지 로드 때 한 번만
  받아오고 이후엔 그때그때의 마우스 이동량만 더해서 로컬로 계속 추적)에
  실시간 마우스 이동량을 더해 매 프레임 `moveTo`를 보내는데, 메인
  프로세스가 그 좌표를 clamp해서 실제 창을 다르게 두면 렌더러의
  `winX`/`winY`는 그 보정을 전혀 모른 채 계속 어긋난 값으로 남음.
  드래그를 끝내고 나면 다음 드래그가 `dragStartWinX = winX`(이 어긋난
  값)에서 새로 시작하니, 화면 끝에 한 번 눌린 이후로는 그 축의 드래그가
  거의 안 움직이는 것처럼 보임(다른 축은 한 번도 clamp된 적이 없어
  정상 동작 — "세로로만 움직인다"는 증상과 정확히 일치). 고침:
  `pointerup`에서 드래그가 끝날 때마다 `getPosition()`으로 실제 위치를
  다시 읽어와 `winX`/`winY`를 재동기화 — 다음 드래그는 항상 진짜 창
  위치에서 시작함. CDP로 화면 밖까지 끌었다가(clamp 유발) 다시 반대
  방향으로 끄는 시나리오를 재현해, 두 번째 드래그의 이동량이 의도한
  델타와 정확히 일치함을 확인.
- **긴 곡명일 때 마퀴 텍스트가 하트 버튼 자리까지 침범하던 문제**: 하트를
  `position:absolute; right:0`으로 얹고 `.title`에 `padding-right`로
  자리만 남겨뒀었는데, `padding`은 정상 흐름 레이아웃에서나 "예약된
  자리"일 뿐 — `overflow:hidden`의 클리핑 경계는 padding box 기준이라,
  `white-space:nowrap`인 매우 긴 텍스트(특히 마퀴 애니메이션으로
  `translateX`까지 걸리면 더욱)는 그 padding 영역 안까지 얼마든지
  그려짐. 즉 하트가 있어야 할 자리가 "비어있게 강제되는" 게 아니라
  그냥 "다른 걸 안 놓기로 한 자리"였을 뿐이라, 텍스트가 거기까지
  차오르면 하트를 가려버렸음. 고침: `.title`을 flex 컨테이너로 바꾸고
  마퀴 텍스트를 `.title-scroll`(flex:1, 자체 `overflow:hidden`)이라는
  진짜 형제 박스 안에 넣음 — flex는 형제마다 독립된 클리핑 경계를 주기
  때문에, 텍스트가 아무리 길어도 자기 몫(`.title-scroll`의 실제
  `clientWidth`, 하트가 차지한 만큼 자동으로 줄어듦) 밖으로는 못
  그려짐. 마퀴 스크롤 거리 계산(`setupMarquee()`)도 `.title` 전체가
  아니라 이 `.title-scroll`의 `clientWidth` 기준으로 바꿔서, 스크롤이
  실제로 하트 쪽으로는 절대 넘어가지 않도록 함. 실제 텍스트가 화면 밖
  까지 늘어나도 하트와 겹치지 않는 것을 좌표로 확인.

## 8. 빌드/배포

- `npm run dev` / `npm start` — 개발 모드(HMR).
- `npm run build` — electron-vite 프로덕션 빌드.
- `npm run dist` — electron-builder로 Windows용 두 가지 산출물 생성
  (`release/`):
  - NSIS 설치 프로그램
  - 포터블 단일 실행 파일(설치 불필요)
- 앱 아이디: `com.example.spoti`, 제품명: `SpotI`.

## 9. 테스트

- `npm test` (`vitest run`) — 2개 스위트, 13개 테스트.
  - `popupController.test.ts` (6개): 정상 호버, grace 구간 복귀,
    fade-out 도중 복귀(원래 버그 재현), 초당 다회 토글 300회, 다초 간격
    토글 20회, 1ms~3s 무작위 토글 1000회.
  - `pollingService.test.ts` (7개): 재생/정지 경합 수정 회귀 테스트 4개
    — 액션 이전에 조회를 시작한 폴링이 낙관적 방송을 덮어쓰지 않는지,
    액션 직후 `pollNow()`가 전파 지연 중인 옛 값을 가져와도 깜빡이지
    않는지(실제 버그 재현), 가드 윈도 중에도 트랙 변경 같은 진짜 변화는
    반영되는지, 가드 윈도가 지난 뒤엔 외부 변경이 정상 반영되는지.
    캐시 신선도/방송 gating 수정 회귀 테스트 3개 — 트랙·재생 여부가
    안 바뀌어도 `getLastKnownState()`의 볼륨이 최신으로 갱신되는지,
    셔플만 바뀌어도 방송이 나가는지, 반복만 바뀌어도 방송이 나가는지.
- 그 외 기능은 `npm run typecheck` + 개발 모드 relaunch 후 콘솔 에러
  부재 확인 방식으로 검증(자동화된 E2E 테스트는 아직 없음).
